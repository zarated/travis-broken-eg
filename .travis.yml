language: node_js
node_js:
- "6.1"
notifications:
  email:
    on_success: never
    on_failure: never
before_script:
  - echo 'running before script'
  - npm install -g gulp
  - npm install -g karma
  - npm install -g mocha
  - npm install -g bower
script:
  - npm install &> /dev/null
  - echo 'running script'
after_script:
  - echo 'running after script'
after_success:
  - echo 'running after success'
